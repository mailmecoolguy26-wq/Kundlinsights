'use strict';const test=require('node:test');const assert=require('node:assert/strict');const {AppleSignedDataVerifierFactory,AppleSignedDataVerifier}=require('../../src/payment/apple/apple-signed-data-verifier');
test('Apple verifier delegates only to injected official verifier with configured roots',async()=>{let args;class Mock{constructor(...v){args=v;}async verifyAndDecodeTransaction(v){assert.equal(v,'signed');return {transactionId:'verified'};}}const factory=new AppleSignedDataVerifierFactory({rootCertificateProvider:{load:()=>[Buffer.from('root')]},bundleId:'com.kundlinsights.test',SignedDataVerifierClass:Mock});assert.deepEqual(await new AppleSignedDataVerifier({factory}).verifyAndDecodeTransaction({signedTransaction:'signed',environment:'SANDBOX'}),{transactionId:'verified'});assert.equal(args[3],'com.kundlinsights.test');await assert.rejects(new AppleSignedDataVerifier({factory}).verifyAndDecodeTransaction({signedTransaction:'',environment:'SANDBOX'}),(e)=>e.code==='APPLE_JWS_MALFORMED');});
