'use strict';const test=require('node:test');const assert=require('node:assert/strict');const {createProductCatalog}=require('../../src/payment');
test('Apple catalog mapping is configuration-injected and safely absent by default',()=>{const catalog=createProductCatalog({appleCareerPremiumAnnualProductId:'com.kundlinsights.test.career.annual'});assert.equal(catalog[0].providerProductIds.APPLE,'com.kundlinsights.test.career.annual');assert.equal(createProductCatalog()[0].providerProductIds.APPLE,null);assert.equal(catalog.some(x=>x.providerProductIds.APPLE==='unknown'),false);});
