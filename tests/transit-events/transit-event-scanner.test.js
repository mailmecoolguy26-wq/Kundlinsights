'use strict';const test=require('node:test'),assert=require('node:assert/strict');const {calculateRashiHouses}=require('../../src/bhava');const {scanTransitEvents}=require('../../src/transit-events');const names=['Sun','Moon','Mars','Mercury','Jupiter','Venus','Saturn','Rahu','Ketu'];function body(x,m='direct'){return {siderealLongitudeDegrees:x,motion:m,longitudeSpeedDegreesPerDay:m==='retrograde'?-1:1};}const natal={Ascendant:body(330),Sun:body(210),Moon:body(300),Mars:body(30),Mercury:body(210),Jupiter:body(90),Venus:body(210),Saturn:body(240),Rahu:body(270,'retrograde'),Ketu:body(90,'retrograde')};const houses=calculateRashiHouses({ascendantCanonicalSiderealLongitude:330,bodies:natal});const engine={calculate(input){const t=Date.parse(`${input.date}T${input.time}Z`);const h=(t-Date.parse('2024-01-01T00:00:00Z'))/3600000;const bodies=Object.fromEntries(names.map(n=>[n,body(n==='Jupiter'?29+h:100,n==='Jupiter'&&h>2?'retrograde':'direct')]));return {bodies,calculationStatus:'PROVISIONAL'};}};test('detects refined ingress and preserves deterministic provisional provenance',()=>{const r=scanTransitEvents({startInstant:'2024-01-01T00:00:00.000Z',endInstant:'2024-01-01T02:00:00.000Z',natalBodies:natal,natalHouses:houses,astronomicalEngine:engine,observer:{latitude:0,longitude:0},eventTypes:['rashiIngress']});assert.equal(r.events.length,1);assert.equal(r.events[0].body,'Jupiter');assert.equal(r.events[0].toRashi.sanskritName,'Vrishabha');assert.equal(r.provenance.calculationStatus,'PROVISIONAL');assert.equal(Object.isFrozen(r),true);});
