'use strict'; const { hash, freeze, assertNoForbidden } = require('../synthesis/evidence-node'); const { CONCLUSION_STATUSES, RULE_CLASSIFICATIONS, FORBIDDEN_FIELDS, INTERPRETATION_RULESET_ID } = require('./reference-data');
function forbidden(value) { if (!value || typeof value !== 'object') return; for (const [key, child] of Object.entries(value)) { if (FORBIDDEN_FIELDS.includes(key)) throw new RangeError(`Forbidden interpretation field: ${key}`); forbidden(child); } }
function conclusion(input = {}) { forbidden(input); if (!CONCLUSION_STATUSES.includes(input.conclusionStatus) || !RULE_CLASSIFICATIONS.includes(input.ruleClassification)) throw new RangeError('Invalid conclusion status or rule classification.'); const arrays = ['evidenceIds','rootSourceIds','evidenceFamilyIds','independentMechanismFamilies','sourceStrengths','contradictions','unresolvedEvidenceIds','missingData']; arrays.forEach((key) => { if (!Array.isArray(input[key])) throw new TypeError(`${key} must be an array.`); }); const identity = { rulesetId: input.rulesetId, interpretiveRuleId: input.interpretiveRuleId, domain: input.domain, topic: input.topic, status: input.conclusionStatus, evidenceIds: [...new Set(input.evidenceIds)].sort() }; return freeze({ conclusionId: `conclusion:${hash(identity)}`, domain: input.domain, topic: input.topic, rulesetId: input.rulesetId || INTERPRETATION_RULESET_ID, interpretiveRuleId: input.interpretiveRuleId, ruleClassification: input.ruleClassification, conclusionStatus: input.conclusionStatus, ...Object.fromEntries(arrays.map((key) => [key, [...new Set(input[key])].sort()])), temporalContext: input.temporalContext || { dashaIntervals: [], gocharSnapshotInstant: null, transitEventIds: [] }, provenance: { ...input.provenance, infrastructureRulesetId: INTERPRETATION_RULESET_ID, astronomyCalculation: 'not-performed', ayanamshaCalculation: 'not-performed', houseCalculation: 'not-performed', vargaCalculation: 'not-performed', dashaCalculation: 'not-performed', gocharCalculation: 'not-performed', transitScanning: 'not-performed', ashtakavargaCalculation: 'not-performed', predictionGeneration: 'not-performed', naturalLanguageGeneration: 'not-performed', remedyGeneration: 'not-performed' } }); }
module.exports = { createConclusion: conclusion, assertNoForbiddenInterpretationFields: forbidden };
