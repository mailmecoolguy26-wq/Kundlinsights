'use strict';
const { repositoryError, immutableCopy } = require('../persistence/contracts');
const { getProduct, CAREER_PROFILE_UNLOCK_SKU } = require('./index');
const { ProfileUnlockAssignmentService } = require('./profile-unlock-assignment-service');
function fail(code) { throw repositoryError(code); }
class PurchaseProviderRegistry { constructor(adapters = {}) { this.adapters = adapters; } resolve(provider) { const adapter = this.adapters[provider]; if (!adapter) fail('PURCHASE_PROVIDER_UNSUPPORTED'); return adapter; } }
class FakePurchaseProvider {
  constructor({ enabled = process.env.NODE_ENV !== 'production' } = {}) { this.enabled = enabled; }
  async verify({ provider, environment, productId, evidence }) { if (!this.enabled) fail('PURCHASE_PROVIDER_UNSUPPORTED'); if (!evidence || evidence.kind !== 'FAKE_PURCHASE' || typeof evidence.transactionId !== 'string') fail('PURCHASE_EVIDENCE_INVALID'); return { provider, environment, productId, providerTransactionId: evidence.transactionId, originalTransactionId: evidence.originalTransactionId || evidence.transactionId, status: evidence.status || 'ACTIVE', purchasedAt: evidence.purchasedAt, validFrom: evidence.validFrom, validUntil: evidence.validUntil, providerEventTime: evidence.providerEventTime || evidence.purchasedAt }; }
  async restore(input) { if (!Array.isArray(input.evidence && input.evidence.items)) fail('PURCHASE_RESTORE_FAILED'); return Promise.all(input.evidence.items.map((evidence) => this.verify({ ...input, evidence })) ); }
}
class PurchaseVerificationService {
  constructor({ authUserResolver, repositories, unitOfWork = null, registry, careerAccessResolver, profileUnlockAssignmentService = null, idGenerator, clock } = {}) { this.authUserResolver = authUserResolver; this.repositories = repositories; this.unitOfWork = unitOfWork; this.registry = registry; this.careerAccessResolver = careerAccessResolver; this.profileUnlockAssignmentService = profileUnlockAssignmentService || new ProfileUnlockAssignmentService({ unitOfWork, idGenerator, clock }); this.idGenerator = idGenerator; this.clock = clock; }
  async persistVerified({ principal, normalized, birthProfileId = null }) { const user = await this.authUserResolver(principal); let product; try { product = getProduct(normalized.productId); } catch { fail('PURCHASE_PRODUCT_UNSUPPORTED'); } const repos = this.repositories(); const now = this.clock(); const persistPurchase = async (tx) => tx.purchases.insert({ id: this.idGenerator(), userId: user.id, provider: normalized.provider, environment: normalized.environment, productId: product.logicalSku, providerTransactionId: normalized.providerTransactionId, originalTransactionId: normalized.originalTransactionId, status: 'VERIFIED', purchasedAt: normalized.purchasedAt, validFrom: normalized.validFrom, validUntil: normalized.validUntil, verifiedAt: now, createdAt: now });
    if (product.logicalSku === CAREER_PROFILE_UNLOCK_SKU) {
      if (typeof birthProfileId !== 'string' || !birthProfileId) fail('INVALID_BIRTH_PROFILE_ID');
      const persist = async (tx) => { const purchase = await persistPurchase(tx); const entitlement = await this.profileUnlockAssignmentService.assignInTransaction({ tx, userId: user.id, birthProfileId, purchaseRecordId: purchase.id }); return { purchase, entitlement }; };
      const { purchase, entitlement } = this.unitOfWork ? await this.unitOfWork.run(persist) : await persist(repos);
      const access = await this.careerAccessResolver.resolveForProfile({ repositories: repos, userId: user.id, birthProfileId, at: now });
      return immutableCopy({ purchase: { id: purchase.id, provider: purchase.provider, environment: purchase.environment, logicalProductSku: purchase.productId, productId: purchase.productId, status: purchase.status, purchasedAt: purchase.purchasedAt, validFrom: purchase.validFrom, validUntil: purchase.validUntil }, profileEntitlement: { id: entitlement.id, birthProfileId: entitlement.birthProfileId, logicalSku: entitlement.logicalSku }, entitlement: { career: { eligible: access.eligible, mode: access.mode, consuming: access.consuming, validUntil: access.validUntil } } });
    }
    const persist = async (tx) => { const purchase = await persistPurchase(tx); const subscription = await tx.subscriptions.upsertVerifiedState({ id: this.idGenerator(), userId: user.id, provider: normalized.provider, environment: normalized.environment, productId: product.logicalSku, originalTransactionId: normalized.originalTransactionId, status: normalized.status, validFrom: normalized.validFrom, validUntil: normalized.validUntil, latestPurchaseRecordId: purchase.id, providerEventTime: normalized.providerEventTime, createdAt: now }); return { purchase, subscription }; }; const { purchase, subscription } = this.unitOfWork ? await this.unitOfWork.run(persist) : await persist(repos); const access = await this.careerAccessResolver.resolve({ repositories: repos, userId: user.id, at: now }); return immutableCopy({ purchase: { id: purchase.id, provider: purchase.provider, environment: purchase.environment, logicalProductSku: purchase.productId, productId: purchase.productId, status: purchase.status, purchasedAt: purchase.purchasedAt, validFrom: purchase.validFrom, validUntil: purchase.validUntil }, subscription: { status: subscription.status, validFrom: subscription.validFrom, validUntil: subscription.validUntil }, entitlement: { career: { eligible: access.eligible, mode: access.mode, validUntil: access.validUntil } } }); }
  async verify({ principal, body }) { return this.persistVerified({ principal, normalized: await this.registry.resolve(body.provider).verify(body), birthProfileId: body.birthProfileId }); }
  async restore({ principal, body }) { const values = await this.registry.resolve(body.provider).restore(body); const restored = []; for (const normalized of values) restored.push(await this.persistVerified({ principal, normalized, birthProfileId: body.birthProfileId })); return { restored }; }
  async list({ principal }) { const user = await this.authUserResolver(principal); return (await this.repositories().purchases.listForUser(user.id)).map((p) => ({ provider: p.provider, logicalProductSku: p.productId, status: p.status, purchasedAt: p.purchasedAt, validFrom: p.validFrom, validUntil: p.validUntil })); }
}
module.exports = { PurchaseProviderRegistry, FakePurchaseProvider, PurchaseVerificationService };
